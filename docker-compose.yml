services:
  postgres:
    image: postgres:16-alpine
    container_name: promptyard-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-promptyard}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-promptyard}
      POSTGRES_DB: ${POSTGRES_DB:-promptyard}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-promptyard}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
